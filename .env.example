# iClaude monitored senders configuration
# Add email addresses or iMessage identifiers to monitor
# Separate multiple senders with commas
MONITORED_SENDERS=sender1,sender2,sender3

# Custom prompt to prepend to every message sent to Claude
# This can include instructions, context, or guidelines
CUSTOM_PROMPT=You are a helpful assistant. Keep responses concise.
