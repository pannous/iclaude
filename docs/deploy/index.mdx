---
title: Deployment
description: Run The Companion on remote servers and cloud infrastructure
---

# Deployment

The Companion runs locally by default, but you can deploy it on a remote server for always-on access, shared team usage, or dedicated development environments.

## Deployment options

<CardGroup cols={2}>
  <Card title="GCP Virtual Machine" icon="google" href="/deploy/gcp-vm">
    Deploy on a private GCP Compute Engine VM with Tailscale for secure HTTPS access. Includes full provisioning, NAT setup, and systemd service.
  </Card>
  <Card title="Local Installation" icon="laptop" href="/getting-started/installation">
    Run on your own machine with a single command. Best for individual use.
  </Card>
</CardGroup>

## How it works

Regardless of where you deploy, the architecture stays the same:

1. The Companion server runs on the host (port 3456 by default)
2. It spawns Claude Code or Codex CLI processes as child subprocesses
3. Your browser connects to the server over WebSocket
4. A reverse proxy or tunnel (like Tailscale) provides secure remote access

The server is stateless enough to restart cleanly — sessions persist to disk and CLI processes reconnect automatically. See [Session Recovery](/sessions/session-recovery) for details.

## Choosing a setup

| Scenario | Recommended approach |
|---|---|
| Solo developer, single machine | [Local install](/getting-started/installation) with `bunx the-companion` |
| Always-on server in the cloud | [GCP VM](/deploy/gcp-vm) with Tailscale |
| Isolated per-project environments | [Docker sessions](/environments/docker-sessions) on any host |
| Team sharing a single instance | Remote deploy with Tailscale ACLs for access control |

<Tip>
You can combine approaches — for example, deploy on a GCP VM and use Docker sessions within it for per-project isolation.
</Tip>
